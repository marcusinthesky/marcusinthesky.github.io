# Content

Validated public data shared by the website, feeds, structured metadata, and profile README projection. This package must contain public information only.

Local long-form posts will live in `src/posts` and compile through the web app's headless Fumadocs configuration. Existing writing remains a curated external index until deliberately republished.
